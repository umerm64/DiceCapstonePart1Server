version: '3'
services:
  server:
    container_name: server
    build:
      context: .
    ports:
      - 8080:8080
    volumes:
      - servervol:/serverdata
    networks:
      - backend

networks:
  backend: {}

volumes:
  servervol: {}
